
@model List<ProyectoTest.Models.Imagen>

@{
    ViewBag.Title = "RestoManage";
    Layout = "~/Views/Shared/_Layout_Public.cshtml";
    var idUsuario = ((ProyectoTest.Models.Usuario)Session["Usuario"]).IdUsuario;
}


<head>
    <link rel="icon" href="~/Favicon 16x16.png" />
    <meta name="viewport" content="width=device-width" />
    <title>RestoManage</title>
    @Styles.Render("~/Content/css")
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@100..900&family=Permanent+Marker&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #f6f7f8;
        }
    </style>
</head>

<body style="overflow-x: hidden; background-color: #f1f8ff; ">

    <style>
        * {
            font-family: 'Roboto', sans-serif;
            font-size: 14px;
        }

        body {
            /* Evita que haya un desplazamiento horizontal */
            overflow-x: hidden;
        }

        .SegFila {
            font-size: 15px !important;
            font-weight: 700;
            color: red;
        }

            .SegFila:hover {
                color: gray;
            }

        .custom-button {
            background-color: transparent;
            border: none;
            color: #000;
            padding: 0;
        }

        .custom-card {
            background-color: transparent;
            border: none;
            padding: 0;
        }

        footer {
            background: #222434;
            min-height: 100px;
        }

            footer a {
                color: white;
                font-size: 13px;
            }

        #Header1 {
            height: 70px;
        }

        #Header2 {
            height: 70px;
        }

        #botonI {
            margin-right: -15px;
        }

        .custom-button {
            border: 2px solid #dc3545;
            background-color: white; /* Fondo blanco */
            color: #dc3545; /* Letras rojas */
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s, border 0.3s;
        }

            .custom-button:hover {
                background-color: #dc3545; /* Fondo rojo al pasar el ratón */
                color: white; /* Texto blanco al pasar el ratón */
                border: 2px solid #dc3545;
            }

        .carrusel50 {
            height: 70%;
        }

        .carrusel-img {
            max-height: 50vh; /* Establece la altura máxima al 50% de la altura de la ventana */
            width: auto; /* Permite que el ancho se ajuste automáticamente manteniendo la proporción */
            margin: 0 auto; /* Centra la imagen horizontalmente */
        }

        .carousel-buttons {
            position: relative;
            top: -250px; /* Ajusta esta cantidad según sea necesario */
            text-align: center;
        }


        .carousel-control-prev,
        .carousel-control-next {
            width: 50px; /* Ajusta el ancho según sea necesario */
            height: 50px; /* Ajusta la altura según sea necesario */
            border: none; /* Sin borde */
            top: 50%; /* Posición verticalmente centrada */
            transform: translateY(-50%); /* Centrar verticalmente */
        }



        .carousel-control-prev-icon,
        .carousel-control-next-icon {
            color: black; /* Color del ícono blanco */
            font-size: 1rem; /* Tamaño del ícono */
            transform: scale(1.3); /* Escalar el ícono en un 30% */
            border-width: 2px; /* Grosor de las líneas del ícono */
            filter: invert(108%);
        }
        /* Estilos para el espacio entre las flechas y el carrusel */
        .carousel-control-prev {
            left: 8px; /* Ajusta la posición izquierda según sea necesario */
        }

        .carousel-control-next {
            right: 8px; /* Ajusta la posición derecha según sea necesario */
        }

        .carousel {
            position: relative;
        }

        .carousel-inner {
            display: flex;
        }

        .carousel-item {
            flex: 0 0 100%;
            display: none;
        }

            .carousel-item.active {
                display: block;
            }

        .carousel img {
            width: 100%;
            height: auto;
        }

        .carousel-dots {
            position: absolute;
            bottom: 10px;
            left: 50%;
            transform: translateX(-50%);
        }

        .carousel-dot {
            width: 10px;
            height: 10px;
            background-color: #ccc;
            border-radius: 50%;
            display: inline-block;
            margin: 0 5px;
            cursor: pointer;
        }

            .carousel-dot.active {
                background-color: #333;
            }

        .carousel-control-prev {
            left: -40px; /* Mueve el botón izquierdo más a la izquierda */
        }

        .carousel-control-next {
            right: -40px; /* Mueve el botón derecho más a la derecha */
        }
        /* Estilos para el número aleatorio */
        /* Estilos para el número aleatorio */
        .random-number-badge {
            position: absolute;
            top: 12px;
            left: 26px; /* Agrega margen a la izquierda */
            width: 60px;
            height: 30px;
            background-color: red;
            color: white;
            border-radius: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 16px;
            font-weight: bold;
            z-index: 999;
        }

        .random-number-badge2 {
            position: absolute;
            top: 12px;
            left: 26px; /* Agrega margen a la izquierda */
            width: 100px;
            height: 30px;
            background-color: red;
            color: white;
            border-radius: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 14px;
            font-weight: bold;
            z-index: 999;
        }

        .fs-6 {
            font-size: 0.8rem; /* Define el tamaño de fuente deseado */
        }


        .separator {
            height: 70px;
            width: 1.5px;
            background-color: rgba(0, 0, 0, 0.1); /* Color ligeramente visible */
        }


        .linea-separadora {
            width: 20%;
            margin-top: 10px;
            margin-bottom: 10px;
            margin-left: 12px;
            margin-right: 12px;
            border-top: 3px solid rgba(28, 43, 81, 1); /* Cambia el color según tus necesidades */
            opacity: 1; /* Ajusta la opacidad aquí */
        }

        .titulo-descuentos {
            font-size: 2.5em;
            color: rgba(28, 43, 81,1);
            font-weight: bold;
            display: inline-block;
        }

        .nunito-sans-titulo {
            font-family: "Nunito Sans", sans-serif;
            font-optical-sizing: auto;
            font-weight: 550;
            font-style: normal;
        }

        @@media (max-width: 576px) {
            /* Estilos específicos para pantallas pequeñas aquí */
            /* Reducir la altura de la tarjeta */
            .card {
                height: 100% !important;
            }
            /* Reducir la altura máxima de la imagen */
            .card-img-top {
                height: auto !important;
                max-height: 30vh !important;
            }
            /* Ajusta el ancho de las imágenes al 100% del contenedor padre */
            #catalogo-productos img {
                width: 100% !important;
                height: auto !important;
            }

            #catalogo-productos2 img {
                width: 100% !important;
                height: auto !important;
            }
            /* Reducir la altura de las imágenes del carrusel en modo responsive */
            .carrusel50 .carousel-item img {
                height: 30% !important;
            }

            .carousel-control-prev {
                left: -21px; /* Ajusta la posición izquierda según sea necesario */
            }

            .carousel-control-next {
                right: -21px; /* Ajusta la posición derecha según sea necesario */
            }

            .linea-separadora {
                width: 14%;
                margin-top: 5px;
                margin-bottom: 5px;
                margin-left: 2px;
                margin-right: 2px;
                border-top: 2px solid rgba(28, 43, 81, 1);
            }

            .titulo-descuentos {
                font-size: 1.4em;
                color: rgba(28, 43, 81, 1);
                font-weight: bold;
                display: inline-block;
            }
        }
    </style>



    <div class="sticky-top">
        <!-- HEADER -->
        <div class="container-fluid text-center d-flex justify-content-between align-items-center py-3" style="background-color: #0f172b; ">
            <div style="margin-left: 75px;">
                <!-- Aplicar un margen izquierdo automático para desplazar el contenido a la derecha -->
                <a href="@Url.Action("Index","Public")">
                    <img class="img-fluid" width="170" src="@Url.Content("~/Imagenes/Editables/Logo.svg")" />
                </a>
            </div>

            <div style="margin-left: -5px;">
                <a id="z" class="btn" style="background-color: #feaf39" href="@Url.Action("Index","Login")">
                    <i class="fas fa-sign-in-alt" style="color: #0f172b"></i> <!-- Icono de una persona con una computadora -->
                </a>
            </div>

        </div>

        <nav class="navbar navbar-expand-md navbar-light bg-white py-0 d-none d-md-flex shadow-sm">
            <div class="container" style="height: 70px;">
                <!-- Ajusta la altura según tus necesidades -->
                <div class="d-flex justify-content-between align-items-center w-100">
                    <!-- Utilizamos justify-content-between para distribuir los elementos -->
                    <!-- Aplicando margen automático para centrar los elementos -->
                    <a href="@Url.Action("Index","Tienda")" class="nav-item nav-link text-black fs-5 me-4 flex-grow-1 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/menu.png" alt="Hamburguesa" width="30" height="30" class="mb-1">
                        <span>MENÚ</span>
                    </a>
                    <div class="separator"></div> <!-- Línea vertical separadora -->
                    <a href="#productos-destacados" class="nav-item nav-link text-black fs-5 me-4 flex-grow-1 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/destacados.png" alt="Estrella" width="30" height="30" class="mb-1">
                        <span>DESTACADOS</span>
                    </a>
                    <div class="separator"></div> <!-- Línea vertical separadora -->
                    <a href="#productos-descuentos" class="nav-item nav-link text-black fs-5 me-4 flex-grow-1 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/descuentos.png" alt="Porcentaje" width="30" height="30" class="mb-1">
                        <span>DESCUENTOS</span>
                    </a>
                    <div class="separator"></div> <!-- Línea vertical separadora -->
                    <a href="@Url.Action("Index","Tienda")" class="nav-item nav-link text-black fs-5 me-4 flex-grow-1 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/combos.png" alt="Utensilios" width="30" height="30" class="mb-1">
                        <span>COMBOS</span>
                    </a>
                </div>
            </div>
        </nav>




        <nav class="navbar navbar-light bg-white py-0 d-md-none">
            <div class="container" style="height: 70px;">
                <!-- Ajusta la altura según tus necesidades -->
                <div class="d-flex justify-content-center">
                    <a href="@Url.Action("Index","Tienda")" class="nav-item nav-link text-black fs-6 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/menu.png" alt="Hamburguesa" width="30" height="30" class="mb-1">
                        <span>MENÚ</span>
                    </a>
                    <a href="#productos-destacados" class="nav-item nav-link text-black fs-6 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/destacados.png" alt="Estrella" width="30" height="30" class="mb-1">
                        <span>DESTACADOS</span>
                    </a>
                    <a href="#productos-descuentos" class="nav-item nav-link text-black fs-6 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/descuentos.png" alt="Porcentaje" width="30" height="30" class="mb-1">
                        <span>DESCUENTOS</span>
                    </a>
                    <a href="@Url.Action("Index","Tienda")" class="nav-item nav-link text-black fs-6 d-flex flex-column align-items-center">
                        <img src="~/Imagenes/Editables/combos.png" alt="Utensilios" width="30" height="30" class="mb-1">
                        <span>COMBOS</span>
                    </a>
                </div>
            </div>
        </nav>





    </div>







    <div class="carousel">
        <div class="carousel-inner">
            @foreach (var imagen in Model)
            {
                <div class="carousel-item @if (imagen.Id == 1 && imagen.Activo == true) { <text>active</text> }">
                    <img src="@Url.Content(imagen.RutaImagen)" class="d-block w-100 carrusel-img" alt="...">
                </div>
            }
        </div>
        <div class="carousel-dots"></div>
    </div>






    <section id="productos-descuentos" class="container mt-3 mb-5">
        <div class="container">
            <div style="text-align: center;">
                <hr class="linea-separadora" style="display: inline-block;">
                <h2 class="mb-4 titulo-descuentos nunito-sans-titulo">Descuentos Online <!-- Icono de etiqueta de descuento --></h2>
                <hr class="linea-separadora" style="display: inline-block;">
            </div>

            <div id="catalogo-productos">
                <!-- Contenedor del carrusel -->
                <!-- Aquí va el contenido del listado de productos -->
            </div>
        </div>
        <!-- Botones de navegación del carrusel -->
        <div class="carousel-buttons">
            <button class="carousel-control-prev" type="button" data-bs-target="#productos-carrusel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#productos-carrusel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden next-arrow">Next</span>
            </button>
        </div>
    </section>


    <section id="productos-destacados" class="container mt-3 mb-5">
        <div class="container">
            <div style="text-align: center;">
                <hr class="linea-separadora" style="display: inline-block;">
                <h2 class="mb-4 titulo-descuentos nunito-sans-titulo">Productos Destacados <!-- Icono de etiqueta de descuento --></h2>
                <hr class="linea-separadora" style="display: inline-block;">
            </div>
            <div id="catalogo-productos2">
                <!-- Contenido del listado de productos -->
            </div>
        </div>
        <!-- Botones de navegación del carrusel -->
        <div class="carousel-buttons">
            <button class="carousel-control-prev" type="button" data-bs-target="#productos-carrusel2" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#productos-carrusel2" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden next-arrow">Next</span>
            </button>
        </div>
    </section>


    <!-- Banner para pantallas grandes -->
    <div id="banner-grande" class="d-none d-lg-block mt-4" style="text-align: center;">
        <img src="~/Imagenes/Editables/banner_grande.png" alt="Banner para pantallas grandes" class="img-fluid">
    </div>

    <!-- Banner para celulares -->
    <div id="banner-celular" class="d-block d-lg-none mt-4" style="text-align: center;">
        <img src="~Imagenes/Editables/banner_celular.png" alt="Banner para celulares" class="img-fluid">
    </div>
    <br />




    <footer class="w-100 d-flex flex-column align-items-center justify-content-center" style="background-color: #0f172b; margin-bottom: 0; padding-top: 20px;">
        <div id="iconos" class="mb-3">
            <a class="icon-link icon-link-hover p-0" style="--bs-icon-link-transform: translate3d(0, -.125rem, 0);" href="https://www.facebook.com/profile.php?id=61557277929428">
                <i class="fab fa-facebook-square fa-2x"></i>
            </a>
            <a class="icon-link icon-link-hover" style="--bs-icon-link-transform: translate3d(0, -.125rem, 0);" href="https://www.instagram.com/restomanage?igsh=aXN2cjltaGhmMWNr&utm_source=qr">
                <i class="fab fa-instagram fa-2x"></i>
            </a>
        </div>
        <section class="w-100 text-light mt-2">
            <div class="row w-75 h-20 mx-auto justify-content-center text-center " id="servicios-fila-1">
                <div class="col-lg-4 col-md-12 col-sm-12 d-flex justify-content-center my-0 icono-wrap text-center d-none d-md-block d-lg-block">
                    <div>
                        <div class="d-flex align-items-center mb-2 justify-content-center">
                            <p class="m-0 mr-2 fw-bold ">Link de interés &nbsp &nbsp </p>
                        </div>
                        <div class=" custom-card text-center">
                            <a href="@Url.Action("Termino", "Acerca")" target="_blank" class="text-decoration-none">Términos y condiciones</a> <br>
                            <a href="@Url.Action("Politica", "Acerca")" class="text-decoration-none">Políticas de Privacidad</a> <br>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <p class="text-decoration-none text-light px-3 p-3"> RestoManage &copy; Todos Los Derechos Reservados 2024</p>
    </footer>



    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/js/all.min.js" crossorigin="anonymous"></script>
    <script src="https://unpkg.com/flickity@2/dist/flickity.pkgd.min.js"></script>


</body>



@section scripts{
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const carouselItems = document.querySelectorAll(".carousel-item");
            const carouselDots = document.querySelector(".carousel-dots");

            let currentIndex = 0;
            const intervalTime = 5000; // Cambia el intervalo de tiempo aquí (en milisegundos)
            let slideInterval;

            carouselItems[0].classList.add("active");

            carouselItems.forEach((item, index) => {
                const dot = document.createElement("span");
                dot.classList.add("carousel-dot");
                if (index === 0) {
                    dot.classList.add("active");
                }
                dot.addEventListener("click", function () {
                    showSlide(index);
                });
                carouselDots.appendChild(dot);
            });

            startSlide();

            function startSlide() {
                slideInterval = setInterval(() => {
                    currentIndex = currentIndex < carouselItems.length - 1 ? currentIndex + 1 : 0;
                    showSlide(currentIndex);
                }, intervalTime);
            }

            function showSlide(index) {
                carouselItems.forEach(item => {
                    item.classList.remove("active");
                });
                carouselItems[index].classList.add("active");

                const dots = document.querySelectorAll(".carousel-dot");
                dots.forEach((dot, i) => {
                    dot.classList.remove("active");
                    if (i === index) {
                        dot.classList.add("active");
                    }
                });
            }
        });


    var idUsuarioGlobal = @idUsuario;
    $(document).ready(function () {
        jQuery.ajax({                                      //LLena las categorias
            url: '@Url.Action("ListarCategoria", "Tienda")',
            type: "GET",
            data: null,
            dataType: "json",
            contentType: "application/json; charset=utf-8",
            success: function (data) {
                $(".row-categoria").html("");
                $(".modal-body").LoadingOverlay("hide");
                if (data.data != null) {
                    var html = '';
                    data.data.forEach(t => {
                        html = html + '<option value=" ' + t.IdCategoria + '">' + t.Descripcion + '</option>';
                    });
                    $("#selectCategoria").append(html);
                }
            },
            error: function (error) {
                console.log(error)
            },
            beforeSend: function () {
                $(".modal-body").LoadingOverlay("show");
            },
        });

        $('#selectCategoria').on('change', () => {
            var value = $('#selectCategoria').val();
            var text = $('#selectCategoria option:selected').text();
            var categoriaConcatenada = text;
            if (value != 0) {
                $('#nombreCategoria').text(categoriaConcatenada);
            }
            else {
                $('#nombreCategoria').text('');
            }
            listarProductos(value, '');
            listarProductosAntiguo(value,'');
        });

        $("#txtbuscarproducto").on("input", function () {
            var valorInput = $(this).val();
            if (valorInput.length > 2) {
                listarProductos(0, valorInput);
                listarProductosAntiguo(0,valorInput);
            }
        });

        var direc2 = null;
        $("#divbtn").hide();
        $("#divTienda").hide();
        $("#divcasilla").hide();
        $("#divAutoIngresoDirec").hide();
        $('#selectTipo').val(0);


        listarProductos(0, '');
        listarProductosAntiguo(0,'');
        if (localStorage.getItem("direccion") == null) {
            $('#modalInicial').modal('show');
        }

    });


    $(".btn-ver-categoria").click(function () {
        $('#exampleModal').modal('show');
    });


    function listarCategorias(data) {

        $("#divCategoria").html("");
        var html = '';
        html = html + '<div class="col-md-12"> <ul class="nav nav-pills flex">';

        var acordeon = '';
        acordeon = '<div class="col-md-12">';
        acordeon = acordeon + '<div class="accordion" id="accordionPanelsStayOpenExample">';

        data.forEach(t => {
            html = html + '<li class="nav-item"> ';
            html = html + '<a class="nav-link active" data-bs-toggle="collapse" href="#panelsStayOpen-collapseOne' + t.IdCategoria + '" role="button" aria-expanded="true">"' + t.Descripcion + '"</a> ';
            html = html + '</li> ';

            acordeon = acordeon + '<div class="accordion-item">';
            acordeon = acordeon + '<h2 class="accordion-header">';
            acordeon = acordeon + '<button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseOne' + t.IdCategoria + '" aria-expanded="true" aria-controls="panelsStayOpen-collapseOne' + t.IdCategoria + '">';
            acordeon = acordeon + t.Descripcion;
            acordeon = acordeon + '</button > </h2> <div id="panelsStayOpen-collapseOne' + t.IdCategoria + '" class="accordion-collapse collapse" data-bs-parent="#accordionPanelsStayOpenExample">';
            acordeon = acordeon + '<div class="accordion-body">';
            acordeon = acordeon + '<div class="container px-2 px-lg-2 mt-5"> ';
            acordeon = acordeon + '<div class="class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center"> ';

            t.productos.forEach(t2 => {
                var t222 = "";
                acordeon = acordeon + '<div class="col mb-5"> ';
                acordeon = acordeon + '<div class="card h-100">';
                acordeon = acordeon + '<img class="card-img-top btn-imagen" src="data:image/' + t2.extension + ';base64,' + t2.base64 + '" data-elemento="' + t222 + '"  > ';
                acordeon = acordeon + '<div class="card-body p-4">';
                acordeon = acordeon + '<div class="text-center">';
                acordeon = acordeon + '<h5 class="fw-bolder">';
                acordeon = acordeon + t2.Nombre;
                acordeon = acordeon + '</h5>' + t2.Precio + '</h5> ';
                acordeon = acordeon + '<div class="card-footer p-4 pt-0 border-top-0 bg-transparent">';
                acordeon = acordeon + '<button class="btn btn-outline-dark mt-auto btn-detalle"> Ver Detalles </button>';
                acordeon = acordeon + '<button class="btn btn-outline-dark mt-auto btn-agregar-carrito1"> Agregar al carrito </button>';
                acordeon = acordeon + '</div></div></div></div> </div></div></div></div>';
                acordeon = acordeon + ' ';
            });

            acordeon = acordeon + '';
            acordeon = acordeon + '</div> </div> ';

        });
        html = html + '</ul></div>';
        acordeon = acordeon + '</div> </div> ';

        $("#divCategoria").append(html);
        $("#divCategoria").append(acordeon);

        //$('#exampleModal').modal('show');
    }


    $(document).on('click', '.btn-categoria', function (event) {
        $('#exampleModal').modal('hide');
    });


function listarProductos(_idcategoria, _nombre) {
    jQuery.ajax({
        url: '@Url.Action("ListarProducto", "Tienda")',
        type: "POST",
        data: JSON.stringify({ idcategoria: 1, nombre: _nombre }),
        dataType: "json",
        contentType: "application/json; charset=utf-8",
        success: function (data) {
            $("#catalogo-productos").html("");
            $("#catalogo-productos").LoadingOverlay("hide");
            if (data.data != null) {
                var carruselInnerHtml = '<div class="carousel-inner">';
                var isFirstItem = true;

                // Definir el número de columnas según el tamaño de la pantalla
                var numColumnsLarge = 4; // Por defecto, 4 columnas en pantallas grandes
                var numColumnsSmall = 1; // 2 columnas en pantallas pequeñas

                // Determinar el número de columnas según el tamaño de la pantalla actual
                var numColumns = $(window).width() < 576 ? numColumnsSmall : numColumnsLarge;

                // Iterar sobre los datos de los productos
                for (var i = 0; i < data.data.length; i += numColumns) {
                    carruselInnerHtml += '<div class="carousel-item' + (isFirstItem ? ' active' : '') + '">'; // Agregar una nueva diapositiva al carrusel

                    carruselInnerHtml += '<div class="row justify-content-center g-4">'; // Filas con alineación horizontal centrada
                    function getRandomNumber() {
                        var randomNumber = Math.floor(Math.random() * 7) * 5 + 30; // Genera un número aleatorio múltiplo de 5 entre 30 y 60
                        return '-' + randomNumber + '%';
                    }

                    // Iterar sobre los productos dentro de cada diapositiva
                    for (var j = i; j < i + numColumns && j < data.data.length; j++) {
                        var item = data.data[j];
                        carruselInnerHtml += '<div class="col-lg-3 col-md-4 col-sm-6 col-12 mb-3 position-relative">'; // Columna para cada producto
                        carruselInnerHtml += '<div class="random-number-badge">' + getRandomNumber() + '</div>'; // Añadir el número aleatorio
                        carruselInnerHtml += '<div class="card h-100 text-center">';
                        carruselInnerHtml += '<a href="#" class="btn-imagen" data-elemento=\'' + JSON.stringify(item) + '\'>';
                        carruselInnerHtml += '<img src="' + item.RutaImagen.replace("~", "") + '" class="card-img-top mx-auto" alt="' + item.Nombre + '" style="max-height: 50vh;">';
                        carruselInnerHtml += '</a>';
                        carruselInnerHtml += '<div class="card-body d-flex flex-column justify-content-between">';
                        carruselInnerHtml += '<h5 class="card-title" style="font-weight: bold; font-size: 18px;">' + item.Nombre + '</h5>'; // Nombre en negrita y tamaño de fuente aumentado
                        carruselInnerHtml += '<p class="card-text" style="font-weight: bold; font-size: 18px;">S/. ' + item.Precio + '</p>'; // Precio en negrita y tamaño de fuente aumentado
                        carruselInnerHtml += '<div class="text-center">'; // Contenedor para centrar el botón
                        carruselInnerHtml += '<button class="btn btn-detalle rounded-pill py-1 px-3" style="background-color: #feaf39; width: 170px; color: white;" data-elemento=\'' + JSON.stringify(item) + '\'><b>VER MÁS</b></button>';
                        carruselInnerHtml += '</div>'; // Cierre del contenedor
                        carruselInnerHtml += '</div>'; // Cierre de card-body
                        carruselInnerHtml += '</div>'; // Cierre de card
                        carruselInnerHtml += '</div>'; // Cierre de columna
                    }


                    carruselInnerHtml += '</div>'; // Cerrar la fila
                    carruselInnerHtml += '</div>'; // Cerrar la diapositiva

                    isFirstItem = false;
                }

                carruselInnerHtml += '</div>'; // Cerrar el carrusel-inner

                var carruselHtml = '<div id="productos-carrusel" class="carousel slide">' +
                    carruselInnerHtml

                $("#catalogo-productos").html(carruselHtml);
                $('#productos-carrusel').carousel(); // Inicializar carrusel

            }
        },
        error: function (error) {
            console.log(error)
        },
        beforeSend: function () {
            $("#catalogo-productos").LoadingOverlay("show");
        },
    });
}

// Ejecutar la función cuando la ventana cambie de tamaño
$(window).resize(function () {
    listarProductos(); // Volver a llamar a la función para adaptar el número de columnas
    listarProductosAntiguo(); // Volver a llamar a la función para adaptar el número de columnas
});




    function listarProductosAntiguo(_idcategoria, _nombre) {
    jQuery.ajax({
        url: '@Url.Action("ListarProducto", "Tienda")',
        type: "POST",
        data: JSON.stringify({ idcategoria: 2, nombre: _nombre }),
        dataType: "json",
        contentType: "application/json; charset=utf-8",
        success: function (data) {
            $("#catalogo-productos2").html("");
            $("#catalogo-productos2").LoadingOverlay("hide");
            if (data.data != null) {
                var carruselInnerHtml = '<div class="carousel-inner">';
                var isFirstItem = true;

                // Definir el número de columnas según el tamaño de la pantalla
                var numColumnsLarge = 4; // Por defecto, 4 columnas en pantallas grandes
                var numColumnsSmall = 1; // 2 columnas en pantallas pequeñas

                // Determinar el número de columnas según el tamaño de la pantalla actual
                var numColumns = $(window).width() < 576 ? numColumnsSmall : numColumnsLarge;

                // Iterar sobre los datos de los productos
                for (var i = 0; i < data.data.length; i += numColumns) {
                    carruselInnerHtml += '<div class="carousel-item' + (isFirstItem ? ' active' : '') + '">'; // Agregar una nueva diapositiva al carrusel

                    carruselInnerHtml += '<div class="row justify-content-center g-4">'; // Filas con alineación horizontal centrada

                    // Iterar sobre los productos dentro de cada diapositiva
                    for (var j = i; j < i + numColumns && j < data.data.length; j++) {
                        var item = data.data[j];
                        carruselInnerHtml += '<div class="col-lg-3 col-md-4 col-sm-6 col-12 mb-3 position-relative">'; // Columna para cada producto
                        carruselInnerHtml += '<div class="random-number-badge2">' + "DESTACADO"+ '</div>'; // Añadir el número aleatorio
                        carruselInnerHtml += '<div class="card h-100 text-center">';
                        carruselInnerHtml += '<a href="#" class="btn-imagen2">';
                        carruselInnerHtml += '<img src="' + item.RutaImagen.replace("~", "") + '" class="card-img-top mx-auto" alt="' + item.Nombre + '" style="max-height: 50vh;">';
                        carruselInnerHtml += '</a>';
                        carruselInnerHtml += '<div class="card-body d-flex flex-column justify-content-between">';
                        carruselInnerHtml += '<div class="text-center">'; // Contenedor para centrar el botón
                        carruselInnerHtml += '<br>'; // Contenedor para centrar el botón
                        carruselInnerHtml += '<button class="btn btn-detalle2 rounded-pill py-1 px-3" style="background-color: #feaf39; width: 170px; color: white;"><b>VER TODOS</b></button>';
                        carruselInnerHtml += '</div>'; // Cierre del contenedor
                        carruselInnerHtml += '</div>'; // Cierre de card-body
                        carruselInnerHtml += '</div>'; // Cierre de card
                        carruselInnerHtml += '</div>'; // Cierre de columna
                    }

                    carruselInnerHtml += '</div>'; // Cerrar la fila
                    carruselInnerHtml += '</div>'; // Cerrar la diapositiva

                    isFirstItem = false;
                }

                carruselInnerHtml += '</div>'; // Cerrar el carrusel-inner

                var carruselHtml = '<div id="productos-carrusel2" class="carousel slide">' +
                    carruselInnerHtml

                $("#catalogo-productos2").html(carruselHtml);
                $('#productos-carrusel2').carousel(); // Inicializar carrusel
            }
        },
        error: function (error) {
            console.log(error)
        },
        beforeSend: function () {
            $("#catalogo-productos2").LoadingOverlay("show");
        },
    });
}




    $(document).on('click', '.btn-detalle', function (event) {
        var json = $(this).data("elemento")
        window.location.href = "@Url.Action("Producto", "Tienda")" + "?idproducto=" + json.IdProducto;
    });

    $(document).on('click', '.btn-imagen', function (event) {
        var json = $(this).data("elemento")
        window.location.href = "@Url.Action("Producto", "Tienda")" + "?idproducto=" + json.IdProducto;
    });

    $(document).on('click', '.btn-detalle2', function (event) {
        window.location.href = "@Url.Action("Index", "Tienda")";
});

$(document).on('click', '.btn-imagen2', function (event) {
    window.location.href = "@Url.Action("Index", "Tienda")";
});



    </script>

}
